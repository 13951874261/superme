# 全站功能验证报告（生产模拟测试）

## 测试概述

| 项 | 内容 |
| --- | --- |
| 执行时间 | 2026-08-22 19:15–19:35（UTC+8） |
| 测试环境 | 生产 [https://app.liujingzhuwo.site/](https://app.liujingzhuwo.site/) |
| 账号 / 密码 | User ID `lzhmy` / 解锁秘钥 `1` |
| 执行方式 | Cursor 内置 Browser 自动化：登录 → 顶栏七模块 → 英语六子页签 → 任务中心 API 抽检 |
| 代码修改概述 | **未改产品代码**。本轮只做线上功能验证，记录仍需改建项。 |
| 总测试用例数 | 22 |
| 通过 | 12 |
| 失败 / 阻塞 | 10 |
| 截图目录 | `dist/e2e-verify/` |

**入口（共用）**

- 访问地址：`https://app.liujingzhuwo.site/`
- 前置：密码 `1` 解锁登录，账号保持默认 `lzhmy`

**关键结论（先看这个）**

登录和主界面已经通。后端 API 首字节从上次「全站超时」恢复到 **62ms–1.8s**。当前主问题不再是进不去系统，而是 **今日预生成包缺货 + 主题/画像串数据 + 若干模块空转**。

今日 Cron `run_efabf7f8…`：唤醒 1/1、破绽 1/1、长文 64/64 成功；**精听 0/1 失败**，错误为 `interrupted: server restart`。总进度显示 99%，状态却标成 `completed` / `auditHealth: ok`。

---

## 功能测试用例与执行详情

### A. 登录与首屏

| 编号 | 菜单路径 / 访问地址 | 测试输入 | 预期结果 | 实际结果 | 截图 | 对应需求 |
| --- | --- | --- | --- | --- | --- | --- |
| FV-LOGIN-01 | 打开首页 | GET `/` | 出现解锁页 | **通过** 标题 SUPER AGENT，账号预填 `lzhmy` | （登录后进入主界面） | 登录入口 |
| FV-LOGIN-02 | 解锁登录 | 密码 `1` | 进入主界面 | **通过** 按钮变为「正在初始化…」后进入 `B·AI 高管数字练习场` | `01-home-english.png` | 登录 |
| FV-API-01 | 登录后 `/api/*` | 25 个资源请求 | 3s 内返回 | **通过** 最慢 `/api/vocab/list` 1772ms；`login-ping` 62ms | — | 后端可达 |
| FV-HOME-01 | 英语学习首页 | 无 | 每日唤醒/易错词有今日内容 | **失败** 唤醒「无缓存（lzhmy）」；易错词「暂无缓存」 | `01-home-english.png` | 每日包 |

`GET /api/daily-pack/today?userId=lzhmy` 返回 `{"success":true,"status":"missing"}`。

### B. 英语子页签

| 编号 | 菜单路径 | 测试输入 | 预期结果 | 实际结果 | 截图 | 对应需求 |
| --- | --- | --- | --- | --- | --- | --- |
| FV-EN-DASH | 英语学习 → 进度总控 | 点击 | 路线图、今日长文可用 | **部分失败** 页面出来；路线图 0/10 0%；今日简报 0/1 长文；「智能整理中」空转 | `02-english-dashboard.png` | 进度总控 |
| FV-EN-VOCAB | 英语学习 → 生词复习 | 点击 | 能翻卡复习 | **通过** 今日 1/41 待复习，词条 `TARGET ACQUISITION` | `03-vocab-review.png` | 生词复习 |
| FV-EN-LISTEN | 英语学习 → 精听盲听 | 点击 | 今日音频可听 | **失败** 「今日该组合内容尚未准备好」 | `04-listen-tab.png` | 精听 |
| FV-EN-ROLE | 英语学习 → 多角色练习 | 点击 | AI 开场对练 | **失败** 场景已加载（国际银团贷款谈判），但一直「等待 AI 率先开口」 | `05-roleplay.png` | 多角色 |
| FV-EN-WRITE | 英语学习 → 纵深书面 | 点击 | 起草区可用 | **通过** 空草稿区 + 三维审阅按钮（未提交，避免写脏数据） | `06-write-tab.png` | 书面 |
| FV-EN-SPEECH | 英语学习 → 即兴演讲 | 点击 | 可开始演讲 | **部分失败** UI 就绪；把「英国 (UK)」当成能力短板并开 HIGH-OBSTACLE MODE | `07-speech-tab.png` | 即兴演讲 |

生词复习附带缺陷：例句显示「暂无例句，可直接翻转查看词条。」

### C. 顶栏模块

| 编号 | 菜单路径 | 测试输入 | 预期结果 | 实际结果 | 截图 | 对应需求 |
| --- | --- | --- | --- | --- | --- | --- |
| FV-TAB-LS | 顶栏 → 听读 | 点击 | 理论库 + 场景可见 | **通过** 逻辑谬误库 +《处室风云》剧本 | `08-listen-module.png` | 听读 |
| FV-TAB-SP | 顶栏 → 表达 | 点击 | 框架 + 场景题可见 | **通过** 金字塔结构 + 麦肯锡素材 + 2 分钟即兴题 | `09-speak-module.png` | 表达 |
| FV-TAB-RD | 顶栏 → 精读 | 点击 | 解构工作区可见 | **通过** 空态正常（今日吸收素材 0） | — | 精读 |
| FV-TAB-WR | 顶栏 → 写作 | 点击 | 独立写作模块 | **部分失败** 与英语「纵深书面」几乎同一套 UI | — | 写作 |
| FV-TAB-GT | 顶栏 → 博弈训练 | 点击 | 推送真实高管案例 | **部分失败** 案例能推出，但参与人名为 `E2E-VP-122046`；提示未同步资料抽屉 | `10-gametheory.png` | 博弈 |
| FV-TAB-AE | 顶栏 → 高阶审美 | 点击 | 可选场景进入修炼 | **部分失败** 每日场景已生成，右侧仍挡「请先在左侧选择一个要修炼的情境」 | `11-aesthetics.png` | 审美 |

### D. 任务中心与数据一致性

| 编号 | 菜单路径 / 接口 | 测试输入 | 预期结果 | 实际结果 | 对应需求 |
| --- | --- | --- | --- | --- | --- |
| FV-CRON-01 | `GET /api/daily-cron/runs?userId=lzhmy&days=7` | lzhmy | 今日四模块齐全 | **失败** 精听 `failed:1`；错误 `interrupted: server restart` | 每日定时 |
| FV-CRON-02 | 同上 run 详情 | 主题应与当前练习主题一致 | **失败** 唤醒入参 theme=`新人报到`，页面当前主题=`商务谈判：让步与施压` | 主题对齐 |
| FV-CRON-03 | 运行状态 | 有失败不得标完成 | **失败** `progress:99` 仍 `status:completed`、`auditHealth:ok` | 任务可信度 |
| FV-PROFILE-01 | 侧栏职业轨迹 vs 顶栏 Evolution | 同一用户 | 职位与进度一致 | **失败** 侧栏「总监 / 65%」vs 顶栏「支行副行长 / 45%」 | 画像一致 |

---

## 仍需改建的内容（按优先级）

### P0 — 今日训练闭环断了

1. **每日易错词汇 / 今日唤醒包缺失**  
   首页两块都要求手动刷新。`/api/daily-pack/today` 明确 `missing`。登录 catch-up 已关闭，02:00 Cron 也没把词汇包写进缓存。  
   **改法：** Cron 成功后必须写入 `daily-pack` 与易错词缓存；失败要在首页给「一键补跑」而不是只显示红三角。

2. **今日精听失败且未自动补跑**  
   步骤错误：`interrupted: server restart`。页面只能「后台生成」。  
   **改法：** 进程重启后把 `listen` 失败步标为可重试并自动续跑；不要把整次 run 标成 completed。

3. **多角色练习 AI 不开口**  
   场景、开场句、计时器都在，对话区停在「等待 AI 率先开口…」。  
   **改法：** 进入页签后若 3 秒内无首句，显示失败原因并提供「重新开场」，不要无限待命。

### P1 — 数据串了，看板不可信

4. **Cron 主题用了「新人报到」，页面练的是「商务谈判：让步与施压」**  
   唤醒步骤 `inputs.theme = 新人报到`，fallback 才是商务谈判。  
   **改法：** Cron 选人后必须读当前 `user_theme_prefs.theme`，禁止静默落到「新人报到」。

5. **把 TTS 声线「英国 (UK)」当成能力短板**  
   即兴演讲写「已靶向侦测您的能力短板 英国 (UK)」并打开 HIGH-OBSTACLE MODE；复盘区同样展示「英国 (UK)」。  
   **改法：** 短板画像只允许发音/语法/表达问题；声线偏好不得写入 `user_current_profile` 弱点槽。

6. **职业进度两套数**  
   侧栏 Career Progression：总监 / 65%；顶栏 Evolution：支行副行长 / 45%。  
   **改法：** 单一数据源，两处只读同一画像。

7. **战略路线图 0% 与停留分析 78 篇矛盾**  
   政务轨道 0/10、全场景 0/16，同时右侧写练习 78 篇、词汇 635/846。  
   **改法：** 路线图进度应吃练习记录，或明确标注「本轨道尚未勾选」而不是显示 0%。

8. **任务中心把失败 run 标成完成**  
   `listen.failed=1` 仍 `completed` + `auditHealth:ok` + `progress:99`。  
   **改法：** 任一模块 failed → `status=failed` 或 `partial`，健康度不能 ok。

### P2 — 体验与脏数据

9. **博弈参与人残留 E2E 测试名**  
   `E2E-VP-122046` / `E2E-VP-127472`。生产库混入测试夹具。  
   **改法：** 清理 E2E 夹具；生成时用职位角色名。

10. **博弈未注入资料抽屉**  
    文案：「尚未同步博弈知识，本次训练不注入资料抽屉内容」。

11. **高阶审美选景遮罩逻辑反了**  
    每日场景「政治社交接待会」已出现，右侧仍强制「请先在左侧选择」。点击资料抽屉时出现 `pointer-events: none`。

12. **顶栏「写作」与英语「纵深书面」重复**  
    同一套体制内公文 / 三维审阅。要么合并入口，要么拉开中英写作差异。

13. **进度总控「智能整理中」空转**  
    右侧材料框是空的，左侧仍显示整理中。

14. **生词卡缺例句**  
    `TARGET ACQUISITION` 只有释义，例句占位「暂无例句」。

15. **模块切换后旧页签仍挂在 DOM**  
    听读/表达/精读/写作/博弈/审美全部 keep-alive。功能可用，但会放大卡顿风险。

16. **本周主题与当前练习主题不一致**  
    侧栏「海外信贷谈判与博弈」，英语主题「商务谈判：让步与施压」。需要说明谁覆盖谁，或登录后对齐。

---

## 失败案例分析

| 现象 | 证据 | 根因判断 | 建议 |
| --- | --- | --- | --- |
| 易错词 / 唤醒无缓存 | 首页红三角；`daily-pack/today → missing` | 登录不补跑 + 今日包没落到该账号缓存 | Cron 写缓存；失败露出补跑按钮 |
| 精听不能练 | UI 明确未准备；Cron listen failed | 生成中途 `server restart`，没有 resume | 重启续跑；失败不标 completed |
| AI 角色不开口 | 4s+ 仍「等待 AI 率先开口」 | 开场请求失败或被挂起模块拖死，无超时 UI | 超时提示 + 重试 |
| 短板 = 英国 (UK) | 即兴演讲 HIGH-OBSTACLE；Cron `user_current_profile=英国 (UK)` | 声线字段误写入弱点画像 | 拆字段 |
| 主题跑偏 | wakeup.theme=`新人报到` | 选人/主题查询回落到错误默认值 | 以当前主题表为准 |
| E2E 角色名 | 博弈参与人选择 | 测试数据进生产库 | 清库 + 生成过滤器 |

---

## 具体解决方案（不改代码，等确认）

1. **先修今日闭环：** 补跑 `lzhmy` 的 daily-pack、易错词、精听组合；修 Cron 失败续跑。  
2. **再修数据串味：** 主题、短板画像、职业进度、路线图百分比。  
3. **最后收体验：** 多角色开场超时、审美遮罩、写作入口合并、E2E 脏数据。

未执行破坏性操作（未点「整次重新执行」、未重置今日、未提交评估）。需要我按 P0→P1 逐项改代码时请回复确认。
