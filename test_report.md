# 洞察(听) 思维导图 — 本轮测试报告

## 测试概述

| 项 | 内容 |
| --- | --- |
| 执行时间 | 2026-08-14 23:17（用例编制）；单元测试此前已跑通 |
| 测试环境 | 生产 `https://app.liujingzhuwo.site/`（需 Ctrl+Shift+R）；本地 `npx tsx --test` |
| 代码修改概述 | 听力洞察提交后，在 Markdown 点评上方展示本地递归树导图；支持折叠/缩放/拖拽；导出 SVG、PNG、Markdown、Word（可插 PNG）。未改 Dify 工作流与即兴演讲页。 |
| 总用例数 | 13 |
| 已自动执行 | 1（TC-13 单元测试，7/7 通过） |
| 待手工执行 | 12（浏览器 E2E；当前环境无 Browser 插件，未能自动截屏） |
| 失败数 | 0（就已执行范围） |

**入口（所有页面用例共用）**

- 菜单路径：顶栏 → **洞察(听)**
- 访问地址：`https://app.liujingzhuwo.site/`
- 前置：登录后若模块未挂起，点「洞察(听)」；案例加载完成后再操作

**推荐主路径输入（TC-01～TC-10）**

1. 点 **模拟一键侧写 (调试首选)**（自动填表并跳到第 4 步）
2. 点 **提交导师评估**
3. 等待「DIFY 导师战略点评系统」出现

---

## 功能测试用例

### TC-01 提交后出现导图与点评

- **对应需求**：结果展示层扩展；本地递归树为主路径
- **输入**：一键侧写 → 提交导师评估
- **预期**：
  - 出现标题「DIFY 导师战略点评系统」及 Markdown 点评
  - 点评**上方**出现导图面板，「收起导图」、导出 SVG/PNG/Markdown/Word
  - 画布 `aria-label="洞察思维导图"`
  - 中心节点为真实意图截断（一键数据以「表面肯定老王的工作态度」开头）
  - 一级分支含「表层话术」「利益诉求」「逻辑破绽」；若点评含 `###` 标题，另有对应分支
- **实际结果**：待手工执行
- **截图**：无

### TC-02 面板收起/展开

- **对应需求**：折叠面板不卸载导出逻辑的交互约定
- **输入**：提交成功后点「收起导图」，再点「展开导图」
- **预期**：
  - 收起后画布消失，按钮文案变为「展开导图」，`aria-expanded=false`
  - **导出 SVG / 导出 PNG** 禁用（半透明）
  - **导出 Markdown / 导出 Word** 仍可点
  - 展开后画布恢复，SVG/PNG 恢复可用
- **实际结果**：待手工执行

### TC-03 点击节点折叠子树

- **对应需求**：点击分支折叠其所有子节点，圆点显示 + / −
- **输入**：点击「逻辑破绽」（或任一带 − 的节点），再点一次
- **预期**：子孙隐藏，标记变为 +；再点恢复。中心节点同样可收
- **实际结果**：待手工执行

### TC-04 缩放、拖拽、双击复位

- **对应需求**：画布缩放拖拽；双击空白 fit
- **输入**：在画布空白处拖拽；滚轮缩放；双击空白
- **预期**：可平移、可缩放；双击后树重新适配容器。不触发节点误折叠
- **实际结果**：待手工执行

### TC-05 导出 SVG

- **对应需求**：导出优先级 SVG 第一
- **输入**：导图展开时点「导出 SVG」
- **预期**：下载 `insight-<时间戳>.svg`，可用浏览器打开，含节点文字
- **实际结果**：待手工执行

### TC-06 导出 PNG

- **对应需求**：SVG → PNG
- **输入**：展开时点「导出 PNG」
- **预期**：下载 `insight-<时间戳>.png`，深色底，中文可读（非方框）
- **实际结果**：待手工执行

### TC-07 导出 Markdown 大纲

- **对应需求**：树转 Markdown；不受节点折叠影响（导出完整树）
- **输入**：可先折叠某分支，再点「导出 Markdown」
- **预期**：下载 `insight-<时间戳>.md`；含 `#` 中心、`##` 分支、叶子详情（如滑坡谬误）
- **实际结果**：待手工执行

### TC-08 导出 Word（含导图图）

- **对应需求**：前端 docx，展开时可插 PNG
- **输入**：展开时点「导出 Word」
- **预期**：下载 `insight-<时间戳>.docx`；含标题「洞察导图」、导图图片、层级标题、导师点评正文
- **实际结果**：待手工执行

### TC-09 收起后 Word 仍可导出（无图）

- **对应需求**：收起后 SVG 卸载则不做插图，文本 Word 仍可用
- **输入**：收起导图 → 导出 Word
- **预期**：仍下载 docx；可以没有导图图片，但大纲和点评仍在
- **实际结果**：待手工执行

### TC-10 刷新案例清除导图

- **对应需求**：换题时清掉旧树，不残留上一轮结果
- **输入**：提交出图后点「刷新案例」
- **预期**：点评区和导图面板消失；新案例加载；未再提交前无导图、无导出按钮
- **实际结果**：待手工执行

### TC-11 接口失败仍生成本地树

- **对应需求**：本地树是主路径，失败分支也建树
- **输入**：可在开发者工具将 Dify 洞察请求阻断后，一键侧写并提交
- **预期**：出现「解析失败」类 Markdown；**仍有导图**（中心为真实意图截断）；导出 Markdown/Word 可用
- **实际结果**：待手工执行（需模拟断网或拦截请求）

### TC-12 回归：即兴演讲页未改

- **对应需求**：本轮不改 `ImpromptuSpeechTab`
- **输入**：顶栏 → 英语引擎 → 即兴演讲（若入口存在）
- **预期**：仍为原静态卡片导图（中心 + 分支关键词），无本轮 SVG 导出条
- **实际结果**：待手工执行

### TC-13 单元测试（已执行）

- **对应需求**：builder / Markdown 大纲 / docx blob
- **菜单路径**：本地命令，非页面
- **输入**：

```bash
npx tsx --test src/utils/insightMindMapBuilder.test.ts src/utils/mindMapExport.test.ts src/utils/insightWordExport.test.ts
```

- **预期**：7 项通过
- **实际结果**：**通过**（7 pass / 0 fail）
- **截图**：无（命令行）

---

## 失败案例分析

当前无已执行失败项。E2E 12 条尚未用浏览器插件跑通，不能视为页面验收通过。

## 具体解决方案 / 执行建议

1. 生产页强制刷新：`https://app.liujingzhuwo.site/` → Ctrl+Shift+R → **洞察(听)**。
2. 优先跑 TC-01、TC-02、TC-05～TC-08，覆盖主路径与四种导出。
3. 若 PNG 中文乱码，检查 SVG 导出字体是否写入 `Microsoft YaHei`。
4. 若提交后无导图，看控制台是否 `buildInsightMindMap` 报错，以及 `feedback` 是否已设置。
5. 模块若提示「已暂时挂起」，先完成复盘调度中的主攻模块再测。
