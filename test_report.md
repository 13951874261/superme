# test_report.md

## 测试概述

| 项 | 内容 |
|----|------|
| 测试时间 | 2026-08-05 |
| 测试环境 | 本地/生产 `https://ai.234124123.xyz` 或 `https://app.liujingzhuwo.site/` |
| 代码修改概述 | 词汇矩阵×艾宾浩斯同源轻量队列、cache-first、侧栏虚拟列表+悬停展开、DB 索引、`/review?light=1` `/list?light=1` `/item/:id` |
| Browser 插件 | **不可用**（当前会话无 Browser MCP）；下列用例供人工/后续自动化执行 |
| 总用例数 | 4（建议） |
| 已自动执行 | 0 |
| 成功/失败 | 待测 |

## 功能测试用例

### TC-1 词汇矩阵毫秒级首屏（有缓存）

| 项 | 内容 |
|----|------|
| 菜单路径 | 英语战略 → 词汇矩阵 |
| 访问地址 | https://app.liujingzhuwo.site/ |
| 测试数据 | 先进入一次词汇矩阵或展开生词本以写入 `sessionStorage.sa_vocab_review_light_v1`，再切走后切回 |
| 预期结果 | &lt;50ms 出现卡片或空态；可短暂显示「同步中…」；无永久「正在检查今日待复习词条…」 |
| 对应需求 | P3 cache-first |
| 实际结果 | 待测 |
| 截图 | — |

### TC-2 两侧今日复习数量同源

| 项 | 内容 |
|----|------|
| 菜单路径 | 侧栏艾宾浩斯（同分区）↔ 词汇矩阵 |
| 测试数据 | 政商务区；对比侧栏「复习 N」与矩阵「今日复习模式 — N 个」 |
| 预期结果 | 同分区下 N 一致（均来自 `/review?light=1`，无主题强制过滤） |
| 对应需求 | 内容一致 + 无主题过滤 |
| 实际结果 | 待测 |

### TC-3 侧栏词条悬停/点击展开全文

| 项 | 内容 |
|----|------|
| 菜单路径 | 侧栏 → 展开艾宾浩斯生词本 |
| 测试数据 | 长词条（原显示为 `Fo...` / `If t...`） |
| 预期结果 | 悬停或点击词头后完整显示 word/音标/释义；默认仍单行 |
| 对应需求 | D2 |
| 实际结果 | 待测 |

### TC-4 后端索引与轻量接口

| 项 | 内容 |
|----|------|
| 菜单路径 | 服务端 |
| 测试数据 | `EXPLAIN QUERY PLAN SELECT ... WHERE next_review_date <= ? AND repetitions < 999`；`GET /api/vocab/review?light=1` |
| 预期结果 | 计划使用 `idx_vocab_review`；light 响应无完整巨大 payload |
| 对应需求 | DB 索引 + 轻量列表 |
| 实际结果 | 待测（需服务重启后） |

## 失败案例分析

（部署并人工执行后填写）

## 具体解决方案

1. 部署后需 **重启** `super-agent-vocab` 以使索引与新路由生效。  
2. 浏览器 **Ctrl+Shift+R**。  
3. 若 Browser MCP 可用，可按 TC-1～3 自动截屏补全本报告。
