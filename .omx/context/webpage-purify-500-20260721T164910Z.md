# Context Snapshot: webpage-purify-500

**Timestamp:** 20260721T164910Z  
**Task:** 解决网页提纯报错（deep-interview）  
**Profile:** standard (threshold ≤ 0.20, max rounds 12)

## Task Statement
用户报告「网页提纯」报错；Network 截图显示生产环境 `POST https://app.liujingzhuwo.site/api/materials/fetch-url` → **500 Internal Server Error**（经本地代理 `127.0.0.1:7897`）。

## Desired Outcome
（待访谈）使网页提取/提纯预览可用，并明确验收标准与环境范围。

## Stated Solution
用户要求 deep-interview 澄清后解决报错；未指定改代码 vs 仅部署 vs 查上游。

## Probable Intent Hypothesis
生产网页提取链路仍失败；可能是上次「Invalid or unexpected token」部署未闭环的延续，或已变成上游/运行时 500。

## Known Facts / Evidence

### [from-code][auto-confirmed]
- 链路：`UrlFetchPanel.tsx` → `POST /api/materials/fetch-url` → `vocab-server/server.js` (~L6783) → `services/webFetcher.js` → `fetchUrlContent`
- 本地 `webFetcher.js`：`node -c` 通过；调用 `${FETCH_ENDPOINT_BASE||https://9router.234124123.xyz/v1}/web/fetch`，body 含 `model: fetch-combo`, `format: markdown`, `max_characters: 0`
- 服务端 catch 后统一 `res.status(500).json({ success: false, error: error.message })`，前端原样展示 `data.error`
- 先前访谈产物：
  - `.omx/context/web-fetch-invalid-token-*`：本地 SyntaxError（文件 UTF-8 损坏）
  - `.omx/specs/deep-interview-web-fetch-still-invalid-token.md`：本地已修，生产未上传；`deploy-webfetcher-putty.ps1` 待 SSH 执行

### [from-user] (screenshot, this session)
- 失败请求：`https://app.liujingzhuwo.site/api/materials/fetch-url`，Method POST，Status **500**
- 同会话旁有 `pack-for-llm`、`generate-material`、`task_material_*` 等请求
- Response body（Round 2）：`{"success":false,"error":"fetch failed"}`

### [from-code] inference notes
- `error: "fetch failed"` 与未捕获的 Node `fetch()` 网络失败文案一致（DNS/连接拒绝/TLS/超时等），**不是**先前的 `Invalid or unexpected token`（SyntaxError）
- 说明生产机已能 `require(webFetcher)`；失败点更可能在出站调用 `9router.../v1/web/fetch`
- 浏览器经 `127.0.0.1:7897` 代理访问站点，**不代表**服务器出站也走同一代理

## Constraints
- API Key 不暴露前端（既有约定）
- 部署：vocab-server systemd + nginx `/api/` 反代（deployment-notes）
- `omx` CLI 本机不可用 → 用文件快照/纯文本一轮一问代替 `omx state` / `omx question`

## Unknowns / Open Questions
- 成功定义：仅生产预览通？任意 URL？特定站点？
- 500 的 `error` 文案是什么（UI 红字 / Response JSON）？
- 本轮是否必须部署到 `app.liujingzhuwo.site`？是否允许 SSH/PuTTY？
- Non-goals / Decision Boundaries 未声明

## Decision-boundary unknowns
- 可否改上游 endpoint / 换抓取实现？
- 可否只部署不改代码？
- 错误提示是否要产品化（隐藏上游细节）？

## Likely Codebase Touchpoints
- `vocab-server/services/webFetcher.js`
- `vocab-server/server.js` (`/api/materials/fetch-url`)
- `src/components/UrlFetchPanel.tsx`
- `scripts/deploy-webfetcher-putty.ps1` / `deploy-smart`

## Relevant Repo Docs / Context Inspected
- `detail.md`（网页提取接口）
- `docs/user_maual.md`（网页提取）
- `.omx/context/web-fetch-invalid-token-*`, `web-fetch-still-invalid-token-*`
- `.omx/specs/deep-interview-web-fetch-*.md`
- workspace deployment-notes rule
- No `AGENTS.md` in repo root

## Terminology / Doc-Code Notes
- 用户说「网页提纯」；文档/UI 多用「网页提取」；同一链路 `/api/materials/fetch-url`
- 「提纯」也可能泛指材料管线（pack-for-llm / generate-material）；截图证据指向 fetch-url 500

## Prompt-safe Initial-context Summary Status
`not_needed`
