# 访谈记录：刷新/重新生成后词汇去重

- slug: `dedupe-refresh-vocab`
- 时间: 2026-08-20T13:50:00Z
- 档位: standard（阈值 0.20）
- 最终模糊度: 0.08（6 轮收敛）
- 规格产出: `.omx/specs/deep-interview-dedupe-refresh-vocab.md`
- 上下文快照: `.omx/context/dedupe-refresh-vocab-20260820T135000Z.md`

## 初始诉求

"每日唤醒和破绽词汇中【刷新】或【重新生成】后不允许重复。"

## 轮次明细

### 前置代码勘察（[from-code][auto-confirmed]，未占用提问轮次）
定位到两模块共用 `POST /api/daily-pack/regenerate` 与同一 Dify 唤醒工作流；确认排除名单 `history_exclude` 仅含生词本词条；确认 `buildFlawDisplayWords` 的 `sessionExclude` 恒传空数组；确认无任何"已推送词"持久化。

### 第 1 轮 | Scope | 模糊度 85% → 62%
问：去重的比较范围多大（会话内 / 当天 / 滚动 N 天 / 永久）
答：**滚动窗口（近 N 天）**
影响：排除了内存变量方案，确定必须持久化；同时避免永久去重的词库枯竭风险。

### 第 2 轮 | Constraint | 62% → 45%
问：去重过滤后数量不足（唤醒<10 / 破绽<6）如何处理
答：**先把重复词追加进排除名单重调 LLM 一次；仍不足则用最久未出现的旧词补齐至满数，并在界面提示**
影响：确立"数量优先于绝对不重复"的取舍基调。

### 第 3 轮 | Scope | 45% → 32%
问：两个模块共用一个历史池还是各自独立
答：**共享一个池**
影响：历史表需带 `module` 字段用于溯源，但查询时不按模块过滤。

### 第 4 轮 | Scope（Pressure Pass） | 32% → 22%
问：去重在哪些生成入口生效（用具体场景回压用户最初"刷新后"的措辞）
答：**所有生成入口 —— cron 预生成 + 手动刷新都去重，共用同一历史池**
影响：这是本次访谈价值最高的一轮。用户原始表述只覆盖手动刷新，实际需求覆盖 cron 路径，实现范围从 1 处扩到 3 处。

### 第 5 轮 | Non-goals（强制门） | 22% → 12%
问：哪些改动必须排除
答：**6 项全部排除**（Dify 工作流 / UI 改版 / 历史管理界面 / 生词本 / 其他子模块 / daily_packs 缓存重构）
追加：**新表须建索引提升查询效率**（正向要求，已计入 in-scope）

### 第 6 轮 | Decision Boundaries（强制门） | 12% → 8%
问：哪些参数授权直接决定
答：**6 项全部授权**（窗口天数 / 归一化规则 / 重试次数 / 表结构索引 / 清理策略 / 提示文案）

## 收敛判定

- 加权模糊度 0.08 ≤ 阈值 0.20 ✓
- Non-goals 明确 ✓
- Decision Boundaries 明确 ✓
- Pressure Pass 完成（第 4 轮）✓
- 闭合审计：剩余不确定项（如具体文案措辞）已被授权，不再改变实现路径 ✓
