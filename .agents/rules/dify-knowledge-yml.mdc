---
globs: "**/*.{yml,yaml}"
alwaysApply: false
---
# Dify 方案、YAML 与知识库写入

设计、修改或导出 **Dify 相关能力**（工作流、应用编排、知识库对接）时，必须先对照并符合官方思路，再落到 **可版本化的 YAML**（如 Dify 工作流导出或本仓库中的 `*.yml` 编排），禁止仅凭臆测字段名或路径。

## 必读文档（权威来源）

- **产品与编排语境**：[Dify 介绍（中文）](https://docs.dify.ai/zh/use-dify/getting-started/introduction) — 理解应用/工作流/知识库等概念后再改 YAML 或节点逻辑。
- **条件分支节点**：[Conditional Branch IF/ELSE](https://docs.dify.ai/en/use-dify/nodes/ifelse)（英文；中文版可在文档内切换语言）— 变量类型与比较值须一致。
- **LLM 节点（含 Context）**：[LLM](https://docs.dify.ai/versions/3-0-x/en/user-guide/workflow/node/llm) — 工作流里如何把上游文本注入提示词、Context 变量与 `{{#context#}}`。
- **知识库写入 API**：[从文本创建文档](https://docs.dify.ai/api-reference/%E6%96%87%E6%A1%A3/%E4%BB%8E%E6%96%87%E6%9C%AC%E5%88%9B%E5%BB%BA%E6%96%87%E6%A1%A3) — `POST /v1/datasets/{dataset_id}/document/create-by-text`，异步索引需用返回的 `batch` 配合「文档嵌入状态」接口查进度（见该页及索引 API 说明）。

## `https://docs.dify.ai/zh/use-dify/` 全量下级目录到叶子页（索引版）

> 数据来源：官方文档索引 `https://docs.dify.ai/llms.txt`（`/use-dify/` 全路径）。  
> 说明：官方索引当前主要列出 `en/use-dify/*`；使用中文页时可优先尝试将 `en` 替换为 `zh`，若不存在则回退英文页。


### 维护规则（必须执行）

- 修改本规则前，先访问 `https://docs.dify.ai/llms.txt`，重新抽取全部 `/use-dify/` 链接，确保无漏项。
- 记录路径时统一写「无语言前缀、无 `.md` 后缀」的 canonical 形态（如 `/use-dify/nodes/llm`），避免中英文 URL 混写。
- 若要在规则中引用具体章节，优先写中文链接（`/zh/use-dify/...`）；中文页缺失时回退英文（`/en/use-dify/...`）。
- 目录变更后，需同步更新本文件中的“必读文档”和“自检清单”对应链接，避免引用失效。

## 知识库「从文本创建文档」实现要点

- **Base URL**：与部署一致，一般为 `https://<host>/v1`（云托管见文档默认 `https://api.dify.ai/v1`）。
- **鉴权**：`Authorization: Bearer <DATASET_API_KEY>`（密钥仅放服务端/密钥管理，勿写进前端或公开仓库）。
- **请求体（JSON）**：至少 `name`、`text`；向知识库 **添加首个文档** 时 **`indexing_technique` 必填**（`high_quality` 或 `economy`）；可按需设置 `doc_form`、`process_rule`、`doc_language`、`embedding_model*` 等（见 OpenAPI 模式）。
- **本仓库 HTTP 节点约定**：工作流里若用「HTTP 请求」节点发送上述 JSON，**Body 类型用 `raw-text` 绑定已序列化的 JSON 字符串**，勿用会触发「JSON 修复」的 body 类型，以免破坏合法 JSON（例如字段间逗号被误改）。Header 仍使用 `Content-Type: application/json`。

## YAML 产出要求

- 导出/手写 YAML 须与当前 Dify 版本字段兼容，节点、变量、URL 变更时在注释或 `desc` 中说明意图与依赖文档章节。
- 涉及 `dataset_id`、域名、API Key 时：示例可用占位符，**勿提交真实密钥**；合并前检查是否误夹生产凭据。

## 工作流条件判断（IF/ELSE）经验

手写或合并 DSL 时，IF 条件与上游变量的 **类型声明（`varType`）**、**实际运行时类型** 必须一致；否则易出现 **`Invalid actual value type: number or boolean`**（追踪里常见 `conditions: []`），或导入画布时 **`m.replace is not a function`**（前端对非字符串调用了 `.replace`）。

### 开始节点变量命名

- **不要将用户自定义字段命名为 `type`**。`type` 易与节点/段元数据中的类型字段冲突，IF 解析到的「实际值」可能不是下拉框展示字符串，从而触发类型错误。
- 词典/分类类入参建议用 **`dict_type`、`category`** 等与引擎保留语义不撞名的名字；API / 测试运行的 `inputs` 键名与 `variable` 一致。

### `select` 与 IF 之间建议加「规范化」代码节点（尤其 1.13.x）

- 开始节点 **`select`** 在运行时可能不是纯字符串，而是 **整数下标**、**带 `value`/`label` 的对象** 等。
- 在 **IF 之前** 增加代码节点，将入参 **压成稳定形态**，再让 IF 只引用该节点的输出，避免 IF 直连 `开始.xxx` 时出现类型校验失败。
- 推荐同时输出两类字段，分别给 IF 与下游拼装/业务逻辑使用：
  - **`branch_num`（`number`）**：`1` / `2` / `3` 等，专供 IF 做数字相等判断。
  - **`route`（`string`）**：如 `zh_modern`，专供后续代码节点做字符串分支或回写 JSON。

### 与已验证 DSL 对齐的 IF 写法（参考本仓库 `mychat_memory_kb.yml`）

本仓库 **`mychat_memory_kb.yml`** 中「是否写入知识库」分支采用的模式在多款 Dify 版本上较稳：

- 上游 **代码节点** 输出 **`number`** 字段（例如 `do_post`）。
- IF 条件使用 **`comparison_operator: '='`**，**`varType: number`**，**`value` 写作带引号的 `'1'`**（YAML 标量，与引擎期望一致），`variable_selector` 指向 **该代码节点的 number 输出**。

对应片段形态示例（节点 id 以实际导出为准）：

```yaml
conditions:
  - comparison_operator: '='
    value: '1'
    varType: number
    variable_selector:
      - <code_node_id>
      - <number_output_name>
```

- **不要**在 IF 里用未加引号的纯数字 `value: 1` 与 **`varType: string`** 混用去比「开始节点的 select」，除非已确认该版本下运行时值确为字符串。
- 若导入后前端报错 **`m.replace is not a function`**，优先检查 IF 的 **`value`** 是否在 YAML 中被写成 **裸数字**；可改为 **带引号的字符串形式**（与 `mychat_memory_kb.yml` 一致）再试。

### 测试与 API

- 测试运行 / Workflow API 的 **`inputs` 须包含** 开始节点声明的必填项；缺省时 select 可能为 `null`，同样会触发 IF 类型异常。
- 若 DSL 中为 `select` 配置了 **`default`**，仍以面板与版本行为为准；建议在 `desc` 中写明推荐测试 JSON 形状。

### IF 分支后的 LLM 与占位符（词典类工作流已验证方案）

#### 现象（输入与输出无关）

- 用户已在「开始」填写词条，但 LLM 输出释义/主题与词条无关；或追踪里 **`prompts` 只有 `system` 没有 `user`**。
- 追踪 **「数据处理」** 中，`text` 仍含 **未替换** 的 `{{word}}`、`{{#开始节点id.word#}}`、`{{#代码节点id.xxx#}}` 等字面量，模型等于未看到真实词条。

#### 根因归纳

1. **`{{word}}` 等「开始变量名」不注入**：部分自托管/版本下，工作流 LLM 的 `prompt_template.text` 中 **裸 `{{variable_name}}` 不会替换**（与文档示例不一致时，以追踪为准）。
2. **`{{#上游节点.输出字段#}}` 在正文中不替换**：尤其在 **IF-ELSE 之后** 的 LLM，仅拓扑上游的代码节点字段，在提示词字符串里 **可能整段不参与替换**；`开始入参→字符串` 与 LLM 之间隔着 IF 时易出现。
3. **`role: user` 被丢弃**：占位符解析失败或内容被判空时，部分版本 **整条 user 消息不下发**，只剩 system，加剧「无词条」幻觉。

#### 推荐架构（本仓库 `dict_tool_workflow.yml`）

1. **开始 → 代码「规范化」**（如 `开始入参→字符串`）：用 `_s()` 等把 `word`、`dict_type`、`direction` 等压成稳定字符串；输出 `p_word`、`p_ctx`、`p_locale`、`p_dir` 等，供后续 **`value_selector` 显式绑定**（封装类代码节点一直可用此方式）。
2. **IF 前**：`dict_type → route` 代码节点输出 **`branch_num`（number）** + **`route`（string）**；IF 使用 `varType: number` 与 `value: '1'/'2'` 比较 `branch_num`（与 `mychat_memory_kb.yml` 对齐）。
3. **IF 与各分支 LLM 之间**：各加 **一个直连的「透传」代码节点**（仅作数据跳板，逻辑可为恒等/拼字符串）：
   - 入参用 **`variables` + `value_selector`** 指向 **规范化节点**（如 `c0300000`）的 `p_word` 等（代码节点之间不依赖提示词占位符）。
   - 输出 **`lemma_block`（string）**：在 Python 内拼好多行说明（如 `目标词条：…\nuser_context：…\nlocale：…`，英汉分支含 `direction`），避免在 LLM 里分散多个占位符。
4. **LLM 节点**：
   - **`context.enabled: true`**，**`variable_selector`** 指向 **紧邻上游透传节点** 的 **`lemma_block`**（官方说明：普通上游字符串也可填入 Context，仅无引用归因）。
   - **`prompt_template`** 中动态部分 **只使用 `{{#context#}}`** 插入上述整块；人格、JSON Schema、绑定规则等 **其余文案保持静态**，不再在 `text` 里写 `{{#某节点.lemma#}}` 或依赖 `{{word}}`。
5. **图结构**：`IF 某分支 true/false → 透传节点 → 该分支 LLM`，保证 LLM 的 **直接前驱** 为透传节点，便于引擎把 Context 与当前节点关联。

#### YAML 自检（改完导入后必查）

- [ ] 透传节点 **outputs** 已声明 **`lemma_block`**（`type: string`），且 `main` 的 `return` 含该键。
- [ ] 目标 LLM 的 **`context.enabled`** 为 **`true`**，**`variable_selector`** 为 **`[透传节点 id, lemma_block]`** 二元列表。
- [ ] 对应分支 **edges**：IF 出口 → 透传 → LLM，且无遗漏 `targetHandle`。
- [ ] 追踪中 **透传节点输出** 里 `lemma_block` 已是明文词条；LLM **数据处理** 里 **`{{#context#}}` 已展开**，不再残留 `{{#…#}}`。

#### 与「封装代码节点」的区别

- **封装 / 路由** 等代码节点通过 **`variables` → `value_selector`** 取上游字段，**不依赖** `prompt_template` 里的 `{{#…#}}`。
- **LLM 要看见用户词条** 时，在问题版本上优先走 **Context + `{{#context#}}`**，不要把关键信息只写在 **未被替换的** `text` 占位符里。

#### 导入与 id

- 重新导入 DSL 后若占位符仍字面量，可能是 **画布节点 id 与 YAML 不一致**：在 UI 中打开 LLM，用变量选择器 **重新绑定 Context → 透传.`lemma_block`** 后导出对照。
- 应用 **`version`（DSL）** 须 **≤ 当前 Dify 支持的 DSL 版本**，否则导入告警或行为异常。

## 自检清单

- [ ] 已打开并对照上述官方文档链接中的语义与 API 形状（含 IF/ELSE 文档）。
- [ ] create-by-text 的路径、方法、必填字段与鉴权头与文档一致。
- [ ] 异步场景是否处理 `batch` / 索引状态（若产品需要「写入完成」再往下执行）。
- [ ] 工作流 IF 的 `varType`、`value` 与上游实际输出类型一致；必要时已用代码节点规范化；已与仓库内可参考 YAML（如 `mychat_memory_kb.yml`、`dict_tool_workflow.yml`）对照。
- [ ] 若含 IF 后 LLM：已按上文检查 **透传 + Context + `{{#context#}}`**，追踪确认词条已注入模型。
- [ ] YAML  diff 仅包含本次 Dify/知识库相关变更，无无关重排或密钥泄漏。


字典的api_key:app-zGyrsyvvzHAIO5yx11OcYdpa
聊天api_key:app-TyztRkdBVX4kNUxA8dZ0frk7
知识库的api_key:dataset-Jk5ehEEDT72wmXI5P68hcTlI
