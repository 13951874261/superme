**需求复述**

你选择的是 **A：先输出完整需求规格和实施计划，暂不直接改代码**。

同时你补充了一条重要行为：  
当前台通过筛选条件匹配不到当天预生成内容时，比如凌晨生成失败、文件被清理、数据库记录状态失败等，前台不能只是空提示，而是要提供一个“后台生成”的入口。用户点击后，后端创建异步生成任务，并把任务加入**任务中心**，前台提示用户“已提交后台生成，稍后在任务中心查看”。

**示例确认**

例如用户今天登录后：

```text
用户：u001
当前主题：高管会议
选择条件：
genre = podcast
cefr = C1
duration = 25m
```

前台调用：

```text
GET /api/listen/pregenerated?genre=podcast&cefr=C1&duration=25
```

如果没有命中内容，则页面显示：

```text
今日该组合内容尚未准备好，可提交后台生成。
```

用户点击“后台生成”后：

```text
POST /api/listen/pregenerated/backfill
```

后端创建任务，返回 taskId，并同步进入任务中心。前台提示：

```text
已提交后台生成，请稍后在任务中心查看。
```

---

## 收紧后的需求规格

### 1. 目标

新增后台定时任务，每日凌晨为有效用户预生成英语学习内容，使前台在用户登录、切换主题或切换筛选条件时，优先直接读取当天已生成的长文、精听/盲听文本和音频，减少实时等待。

### 2. 有效用户定义

有效用户必须同时满足：

| 条件 | 说明 |
|---|---|
| 最近登录 | 最近 **7 天内登录过** |
| 有主题记录 | 数据库中存在该用户当前最新主题 |
| 主题有效 | 主题非空、非无效值 |

只为有效用户生成，不为长期未登录用户生成。

### 3. 生成范围

每个有效用户每天只生成该用户的**当前最新主题**。

不生成：
- 用户历史主题
- 用户所有自定义主题
- 系统所有内置主题

### 4. 组合维度

每个用户、每天、当前主题下，生成全部 36 组组合：

```text
genre: meeting / news / podcast
cefr: A2 / B1 / B2 / C1
duration: 5m / 15m / 25m
```

总数：

```text
3 × 4 × 3 = 36 组
```

### 5. 内容类型

每个组合都需要生成两类内容：

| 类型 | 内容 |
|---|---|
| 长文 | 长文正文 + 提纯词汇 + 提纯短语 |
| 精听/盲听 | 截获片段文本 + TTS 音频文件 |

即每个用户每天当前主题下：

```text
36 组长文
36 组精听/盲听音频
```

### 6. 存储方式

采用 **数据库 + 文件系统**。

| 内容 | 存储方式 |
|---|---|
| 元数据 | SQLite 表 |
| 音频文件 | `public/daily_listen_audio/{userId}/...` |
| 长文文件或长文本缓存 | 建议单独目录，如 `public/daily_long_articles/{userId}/...` |
| 状态 | 数据库记录 `ready / generating / failed / missing` |

### 7. 数据记录粒度

长文和音频分开记录。

同一个组合，例如：

```text
userId = u001
theme = 高管会议
genre = meeting
cefr = B1
duration = 15m
date = 2026-07-24
```

数据库中应有两类记录：

| 表/记录类型 | 说明 |
|---|---|
| 长文记录 | 长文正文、词汇、短语、状态、文件路径 |
| 音频记录 | 精听文本、音频路径、TTS 状态、文件路径 |

### 8. 前台命中接口

新增接口：

```text
GET /api/listen/pregenerated
```

查询参数：

```text
userId
theme
genre
cefrLevel
duration
date 可选，默认今天
```

返回当天匹配内容：

```json
{
  "success": true,
  "status": "ready",
  "article": {},
  "audio": {}
}
```

如果未命中：

```json
{
  "success": true,
  "status": "missing",
  "canBackfill": true
}
```

### 9. 未命中时的后台生成

当前台匹配不到具体内容时，提供“后台生成”功能。

新增接口建议：

```text
POST /api/listen/pregenerated/backfill
```

行为：

1. 前台传入当前筛选条件。
2. 后端创建异步补生成任务。
3. 任务进入任务中心。
4. 前台不阻塞等待生成完成。
5. 前台提示“已提交后台生成，请稍后在任务中心查看”。

该逻辑适用于：
- 凌晨生成失败
- 内容状态为 failed
- 数据库有记录但文件不存在
- 内容被清理
- 当天该组合缺失

### 10. 清理策略

清理策略为两者同时生效：

1. 先清理超过 **7 天** 的长文和音频内容；
2. 清理后如果总量仍超过 **1024G**；
3. 继续按最旧内容清理，直到低于 1024G。

容量范围建议覆盖：
- `public/daily_listen_audio`
- `public/daily_long_articles`
- 对应数据库记录

### 11. 前台行为

前台操作时：

| 场景 | 行为 |
|---|---|
| 用户登录 | 读取该用户当天当前主题内容 |
| 切换 genre / cefr / duration | 调 `/api/listen/pregenerated` 命中内容 |
| 命中 ready | 直接展示长文、精听文本、音频 |
| 未命中 missing/failed | 展示后台生成入口 |
| 点击后台生成 | 创建任务中心任务，提示稍后查看 |
| 用户手动重新生成 | 效果与现有手动点击生成保持一致 |

---

## 实施计划

### 阶段 1：数据库设计

新增或扩展表：

1. 用户登录记录表或字段  
用于判断最近 7 天登录过。

2. `daily_listen_articles`  
记录长文内容。

3. `daily_listen_audios`  
记录精听/盲听文本与音频。

4. 任务中心记录复用现有机制  
用于 backfill 后台任务展示。

### 阶段 2：后端预生成服务

新增服务模块，建议：

```text
vocab-server/services/dailyListenPreGenerateService.js
```

职责：

- 查询最近 7 天登录且有主题的用户
- 构造 36 组参数
- 调用现有长文生成逻辑
- 调用现有精听生成逻辑
- 调用现有 TTS 逻辑
- 写入数据库
- 保存文件
- 标记状态

### 阶段 3：定时任务

新增或扩展 cron：

```text
每日凌晨 2:00 Asia/Shanghai
```

流程：

```text
有效用户列表
→ 每个用户当前主题
→ 36 组参数
→ 生成长文
→ 生成精听文本
→ 生成 TTS 音频
→ 写库
→ 清理旧内容
```

### 阶段 4：查询接口

新增：

```text
GET /api/listen/pregenerated
```

按以下条件查询：

```text
userId + theme + genre + cefrLevel + duration + date
```

返回：
- 长文
- 提纯词汇
- 提纯短语
- 精听文本
- 音频 URL
- 状态

### 阶段 5：后台补生成接口

新增：

```text
POST /api/listen/pregenerated/backfill
```

要求：
- 不阻塞前台
- 生成任务进入任务中心
- 复用现有手动生成效果
- 生成完成后写回同一套预生成缓存表

### 阶段 6：前台改造

修改 `ListenTab.tsx`：

- 初始化时优先调用 `/api/listen/pregenerated`
- 筛选条件变化时重新查询缓存
- 命中则直接展示
- 未命中则显示后台生成按钮
- 点击后提交任务中心

### 阶段 7：清理任务

新增清理逻辑：

```text
删除 7 天前内容
→ 统计总大小
→ 若 > 1024G，继续按 created_at 删除最旧内容
→ 同步更新数据库状态或删除记录
```

### 阶段 8：测试用例

后续实现时至少验证：

| 用例 | 路径 | 预期 |
|---|---|---|
| T1 | 最近 7 天登录用户 + 有主题 | 凌晨任务生成 36 组 |
| T2 | 超过 7 天未登录用户 | 不生成 |
| T3 | 前台选择 meeting/B1/15m | 命中对应内容 |
| T4 | 某组合生成失败 | 前台显示后台生成入口 |
| T5 | 点击后台生成 | 任务进入任务中心 |
| T6 | 音频文件缺失 | 查询返回 missing，可 backfill |
| T7 | 超过 7 天文件 | 清理删除 |
| T8 | 总容量超过 1024G | 按最旧内容继续清理 |

---
。